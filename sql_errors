sqlplus -s /nolog <<EOF
    connect SYSTEM/New_15Char_Pswd
    SELECT PROFILE, RESOURCE_NAME, LIMIT FROM DBA_PROFILES
    WHERE RESOURCE_NAME='PASSWORD_LOCK_TIME'
    AND ( LIMIT = 'DEFAULT' OR LIMIT = 'UNLIMITED' OR LIMIT < 1);
    quit
    EOF
